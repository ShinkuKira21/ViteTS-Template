version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/node:lts
      
jobs:
  build-template:

    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          npm install
          npm build

workflows:
  my-custom-workflow:
    jobs:
      - build-template